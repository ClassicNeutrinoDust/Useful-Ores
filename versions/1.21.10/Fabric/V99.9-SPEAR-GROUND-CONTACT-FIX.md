# V99.9 — Spear ground-contact reliability fix

- Preserve the V99.8 working charge damage and sprint behavior.
- Preserve the V99.1-correct jab animation.
- Keep client pre-collision velocity capture based on the Accurate Spears reference.
- Add a client-side swept movement-segment fallback when the crosshair result loses an entity during ground collision.
- Remove the redundant post-collision server view-cone rejection; server still validates the nominated entity, spear state, and physical range.

Goal: eliminate ground sprint impact misses caused by the collision tick changing the attacker/crosshair geometry between client sampling and server processing.
