# Useful Ores V99.8 — 1.21.10 movement compile fix

V99.7 failed under the project's official Mojang mappings because `method_20303()` is an intermediary/Yarn selector and is not a callable Java member in the source namespace.

## Change
- Removed the direct `LocalPlayer.method_20303()` Java call.
- Uses the 1.21.10 source-level `LocalPlayer.isMovingSlowly()` member instead.
- Kept the mixin target selector `method_67270(...)` and the reference movement logic unchanged.

This is strictly a namespace/API correction; no spear animation, charge damage, or movement behavior is intentionally changed beyond making the existing code compile against the project mappings.
